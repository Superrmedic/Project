// const toTop = document.querySelector('.toTop');   // 탑버튼

// toTop.classList.remove('hidden');

// const moveTop = (e) => {
//     e.preventDefault();
//     if (window.pageYOffset > 0) {
//         window.scrollTo({top:0, behavior:"smooth"});
//     }
// }

// toTop.addEventListener('click', moveTop);