'use strict';

const body = document.querySelector('body');
const categoryContainer = body.querySelector('.categoryContainer');
const categoryHover = categoryContainer.querySelectorAll('.categoryHover'); // 카테고리 리스트들

const mainContainer = body.querySelector('.mainContainer');
const slideContainer = mainContainer.querySelector('.slideContainer');
const imgLi = slideContainer.querySelectorAll('.imgLi img');
